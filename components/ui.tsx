import Link from 'next/link';
export function Button({children,href,variant='blue'}:{children:React.ReactNode;href?:string;variant?:'blue'|'gold'|'ghost'}){const cls=variant==='gold'?'bg-gold text-white hover:bg-amber-500':variant==='ghost'?'bg-white text-navy border':'bg-bluebrand text-white hover:bg-blue-700';const el=<span className={`inline-flex items-center justify-center rounded-xl px-5 py-3 font-semibold shadow-sm ${cls}`}>{children}</span>;return href?<Link href={href}>{el}</Link>:el}
export function Badge({children,tone='blue'}:{children:React.ReactNode;tone?:'blue'|'gold'|'green'|'red'|'gray'}){const m={blue:'bg-blue-50 text-blue-700',gold:'bg-amber-50 text-amber-700',green:'bg-emerald-50 text-emerald-700',red:'bg-red-50 text-red-700',gray:'bg-slate-100 text-slate-700'};return <span className={`rounded-full px-3 py-1 text-xs font-bold ${m[tone]}`}>{children}</span>}
export function PageHeader({title,description}:{title:string;description?:string}){return <div className="mb-8"><h1 className="text-3xl font-bold text-navy">{title}</h1>{description&&<p className="mt-2 max-w-3xl text-slate-600">{description}</p>}</div>}
export function StatCard({label,value}:{label:string;value:string|number}){return <div className="card p-5"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold text-navy">{value}</p></div>}
export function EmptyState({title,body}:{title:string;body:string}){return <div className="card p-8 text-center"><h3 className="font-bold text-navy">{title}</h3><p className="mt-2 text-slate-600">{body}</p></div>}
export function LoadingState(){return <div className="animate-pulse rounded-2xl bg-slate-100 p-8 text-slate-500">Loading…</div>}
