import { DashboardShell } from '@/components/nav';import { PageHeader, StatCard } from '@/components/ui';import { createSupabaseServer } from '@/lib/server';export default async function Page(){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();const {data:p}=await s.from('candidate_profiles').select('*').eq('user_id',user?.id).maybeSingle();return <DashboardShell role="candidate"><PageHeader title="Candidate dashboard" description="Track applications and profile readiness."/><div className="grid gap-4 md:grid-cols-3"><StatCard label="Profile completion" value={`${p?.profile_completion_percentage||0}%`}/><StatCard label="Search status" value={p?.employment_search_status||'not set'}/><StatCard label="Applications" value="See list"/></div></DashboardShell>}
