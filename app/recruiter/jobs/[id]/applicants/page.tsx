import { DashboardShell } from '@/components/nav';import { PageHeader } from '@/components/ui';import { ApplicationStatusControls } from '@/components/recruiter';import { createSupabaseServer } from '@/lib/server';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createSupabaseServer();const {data:apps=[]}=await s.from('applications').select('*, candidate_profiles(display_name,headline,profile_completion_percentage)').eq('job_id',id);return <DashboardShell role="recruiter"><PageHeader title="Applicant pipeline" description="Reject, whitelist, open chat, and hire after whitelist."/><div className="grid gap-4">{apps.map((a:any)=><div className="card p-5" key={a.id}><h3 className="font-bold">{a.candidate_profiles?.display_name}</h3><p>{a.candidate_profiles?.headline}</p><ApplicationStatusControls app={a}/></div>)}</div></DashboardShell>}
