import { redirect } from 'next/navigation';import { getSessionProfile } from '@/lib/server';export default async function Page(){const {profile}=await getSessionProfile();if(profile?.primary_role==='candidate')redirect('/candidate/dashboard');if(profile?.primary_role==='recruiter')redirect('/recruiter/dashboard');if(profile?.primary_role==='organisation_admin')redirect('/agency/dashboard');redirect('/auth/login')}
